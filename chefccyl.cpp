//This program will check the shortest path between the cycles
#include<iostream>
using namespace std;
#include<vector>
#include<algorithm>
int main()
{
	int testCases,numOfCycles,queries,sizeOfCycle,val,sizeValue;
	cin>>testCases;
	while(testCases--)
	{
		cin>>numOfCycles>>queries;
		vector<int> cycles[numOfCycles];
		vecotr<int > cyclesConnector;
		vector<int>::iterator it;
        cin>>sizeOfCycle;
        for(int i=0;i<numOfCycles;i++);
        {
        	cin>>sizeValue;
        	for(int j=0;j<val;j++)
        	{
        		cin>>val;
        		cycles[i].push_back(val);
        	}
        }

       for(int i=0;i<sizeOfCycle;i++)
       {
       	   int a,b,c;
       	   cin>>a>>b>>c;
       	   
       }






	}
}
